// read the question carefully ~!
// After partitioning, each subarray has their values changed to become the maximum value of that subarray, it means the value in that subarray will be the maximum one
int maxSumAfterPartitioning(int* A, int ASize, int K)
{
}
